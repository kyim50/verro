# EC2 Deployment Configuration
# Copy this file to deploy.config and fill in your values

# EC2 User (e.g., ubuntu, ec2-user, admin)
EC2_USER="ubuntu"

# EC2 Instance IP Address
EC2_IP="your-ec2-ip-address"

# Path to SSH private key (optional - defaults to ~/.ssh/id_rsa)
# EC2_KEY_PATH="${HOME}/.ssh/your-key.pem"

# EC2 Host (optional - auto-generated from EC2_USER@EC2_IP if not set)
# EC2_HOST="${EC2_USER}@${EC2_IP}"
