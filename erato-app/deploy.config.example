# EC2 Deployment Configuration
# Copy this file to deploy.config and fill in your values
# deploy.config is gitignored and will not be committed

# EC2 Connection Details
EC2_USER="ubuntu"
EC2_IP=""  # Your EC2 instance IP (e.g., "3.18.213.189")
EC2_HOST=""  # Will be auto-generated as ${EC2_USER}@${EC2_IP} if not set
API_DOMAIN="api.verrocio.com"  # Your API domain (optional, for health checks)

# SSH Key Path (leave empty to use ~/.ssh/id_rsa)
EC2_KEY_PATH=""

# Project path on EC2 (leave empty to auto-detect)
PROJECT_PATH=""

# Docker Compose file
COMPOSE_FILE="docker-compose.prod.yml"

# Health Check URL (will be auto-generated as http://${EC2_IP}:3000/health if not set)
HEALTH_CHECK_URL=""

# Health check retry settings
MAX_RETRIES=30
RETRY_DELAY=5

